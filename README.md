# GloryOfKings-Plugin

<div align="center">

![动态访问量](https://count.kjchmc.cn/get/@GloryOfKings-Plugin?theme=rule34)

Yunzai-Bot V3 的王者荣耀插件：战绩查询、赛季表现、皮肤墙、战绩推送、日周月报、跨机器人营地ID共享。

**QQ 交流群：972915804**

</div>

> [!NOTE]
> 本仓库是 [@Tloml-Starry/GloryOfKings-Plugin](https://gitee.com/Tloml-Starry/GloryOfKings-Plugin) 的 Fork。
> 新增皮肤墙、常用英雄、我的英雄、称号墙、趋势图等功能，主页 / 战绩渲染重做成深色主题；
> 原插件只有配置项没实现的**战绩推送 / 上下线提醒**在这里补齐了（订阅制，两个开关独立）。
> 营地接口逻辑参考 [@KimigaiiWuyi/WzryUID](https://github.com/KimigaiiWuyi/WzryUID)，
> 原插件版权与协议归 [@Tloml-Starry](https://gitee.com/Tloml-Starry) 所有。

## 安装

在**云崽根目录**执行。三个源任选一个（内容完全一样，推荐国内的 gitcode 或 gitee）：

**gitcode（国内直连最快）**
```bash
git clone --depth=1 https://gitcode.com/ccxhan/GloryOfKings-Plugin.git ./plugins/GloryOfKings-Plugin/
```

**gitee（国内）**
```bash
git clone --depth=1 https://gitee.com/longhengmu/GloryOfKings-Plugin.git ./plugins/GloryOfKings-Plugin/
```

**GitHub**
```bash
git clone --depth=1 https://github.com/cchanlan/GloryOfKings-Plugin.git ./plugins/GloryOfKings-Plugin/
```

装依赖：

```bash
pnpm install
```

重启 Bot 即可。先 `#绑定营地` 才能查自己的数据。

## 绑定账号

```
#绑定营地 1580886057      营地 ID（营地 App「我的」页可见）
#切换营地 2               多号时切换
#删除营地 2
#营地wx登录               微信扫码登录，拿到更完整的数据权限
#营地QQ登录               QQ 扫码登录（微信区/QQ 区玩家都能用，看你自己绑的营地号）
```

大部分查询指令都支持 `@某人`（对方也得绑过）和直接写营地 ID，比如 `#查询战绩 @张三`、`#排位表现 1580886057`。

## 查战绩与表现

| 指令 | 说明 |
| --- | --- |
| `#查询战绩` `#排位战绩` `#巅峰战绩` | 最近 30 场；点开某场看双方十人评分 / 经济 / 出装 / 参团率 / 五维评级 |
| `#王者主页` | 段位、巅峰分、战力、胜率、皮肤数一张图 |
| `#全部主页` | 名下所有账号的主页 |
| `#排位表现` `#赛季表现` | 段位积分、分路、常用英雄、雷达图、上分趋势 |
| `#巅峰表现` | 巅峰赛五维与全分路数据 |
| `#全部赛季表现` `#全部巅峰表现` | 历届赛季汇总 |
| `#对比 @某人` | 两个人的数据对照 |
| `#谁在打游戏` `#谁在打王者` `#谁在玩王者` `#谁在上号` `#谁在排位` `#王者在线` `#王者在线列表` `#王者在线状态` `#在线列表` | 本群里谁正在对局（含打了多久、段位）、在线、刚打完。名单＝本群成员 ∩ 已绑定营地ID，退群自动出表 |

## 英雄与皮肤

| 指令 | 说明 |
| --- | --- |
| `#常用英雄` | 本赛季场次 / 胜率 / 战力 / 称号 |
| `#我的英雄` | 全部英雄的历史最高战力 / 称号 / 场次 / 胜率 |
| `#称号墙` | 荣耀称号一览（市级 / 小范围榜） |
| `#皮肤墙` `#全部皮肤` | 按价值倒序出图，可 `#皮肤墙 20` 限制数量 |
| `#缺皮肤` | 还差哪些皮肤 |
| `#英雄梯度` | T0~T3 实时强度，可按段位 / 分路筛 |
| `#英雄攻略 李白` | 出装、铭文、核心装备胜率、克制关系 |
| `#皮肤上新` | 新皮肤日历 |

## 趋势与排行

```
#巅峰分趋势        巅峰分曲线（也叫 #上分趋势）
#段位趋势          排位段位变化
#排位排名 #巅峰排名        群内排行，加「刷新」强制重算
#排位总排名 #巅峰总排名     全部已绑用户
```

## 推送订阅

```
#开启战绩推送 / #关闭战绩推送      打完一局自动发全场详情图
#开启上下线提醒 / #关闭上下线提醒    对方上号 / 下号提醒
#开启在线状态 / #关闭在线状态      是否采集你的在线状态（关了就退出 #谁在打游戏 名单，不播报）
#战绩推送状态                   看自己订阅了什么
#开启皮肤上新推送
```

两个开关互相独立。战绩推送只在打完新的一局后触发，不会重复推。

## 战绩报告

```
#日报 #周报 #月报                个人报告
#群日报 #群周报 #群月报            全群汇总（限群管理）
#开启日报推送 / #关闭日报推送       个人订阅（周报、月报同理）
#开启群日报推送                   群订阅
#群报状态
```

报告读的是本地战绩归档，不额外刷营地接口；没打过的时间段不推送。

## 营地ID 共享（可选的跨机器人功能）

你在 A 群的机器人上绑过营地ID，被拉到 B 群（另一个 bot 主人搭的实例）时，
不用重新绑定就能直接查战绩。

**默认不共享**，要自己发指令开：

```
#开启营地ID共享        把你的营地ID传上去，之后换机器人免重绑（#开启营地共享 / #打开营地共享 也行）
#关闭营地ID共享        取消共享（#取消营地共享），之前传上去的也会删掉
#营地ID共享状态        看看自己开没开、共享了哪些号
#同步营地ID共享        把你的绑定传到共享库，别的机器人立刻能查到（只推这一次）
```

上传的是「QQ 号的加盐哈希 → 营地ID」。**不上传登录态、不上传战绩，一个字的凭证都不上传。**

几条要知道的边界：

- **共享库得有人搭**。想自己搭的话，主人发一句 `#营地共享库部署` 就装好了 ——
  服务端代码在仓库的 `server` 分支（gitcode / gitee / GitHub 三个源都有），
  部署指令会把它克隆到 `<云崽根>/data/gok-share-server/` 独立跑（pm2 托管、密钥自动生成、
  装完把令牌私聊给你），更新插件不会动到它的数据；把服务器防火墙（云主机还要加安全组）
  的 8787 端口放行，外面就能连。也可以用朋友搭好的。
  拿到地址和令牌之后三步接上：`#营地共享库地址 <地址>`、`#营地共享库令牌 <令牌>`、
  `#接入营地共享库`。反代、备份、环境变量这些细节看 server 分支的 README。
- **本机要有可用的全局账号**（`#营地wx全局登录` 或 `#营地QQ全局登录`），共享过来的营地ID 才查得动。
- **共享数据会落到本机**：你在别的机器人上绑过的号，本机查过一次之后就会记下来，
  之后 `#查询战绩`、`#排位表现`、`#排位排名`、`#皮肤墙` 这些**全都认**。
  （落下来的那份带来源标记，你 `#关闭营地ID共享` 时会自动清掉。）
- **但它是按需落地的**：没人查过的号不会主动同步过来。所以跨机器的排行榜通常只统计
  「本机查询过的人」，不是全部 —— 想让某个号出现在本机，用它发一次查询就行。
- **序号只认本机**：`#查询2战绩` 里的「2」指的是本机绑定的第 2 个号。
- **撤销几乎立刻生效**：别的机器人下次查询（几秒内）就看不到你的数据了。
- 库里只存 QQ 的**加盐哈希**、不存明文；但接入方拿着自己的令牌、用群成员里的 QQ
  逐个查是能刷出池子的。QQ 号本身不是秘密，介意的话就别开这个功能。

主人侧指令（仅私聊）：

```
#营地共享库              查看接入状态、地址、令牌、缓存条数
#营地共享库部署           一键在本机搭一个共享库，密钥自动生成（#营地共享部署 也行）
#营地共享库状态           看服务端跑没跑、库里有多少人（这条必须带「库」字）
#营地共享库发令牌 <备注>   给别人的机器人签一个令牌；@一下群友就直接私聊发给 TA
#营地共享库接入方          看谁在用你的共享库（#营地共享接入方 也行）
#营地共享库吊销 <序号>     踢掉某个接入方
#营地共享库同步           把你自己和本机开过共享的人一起对上库（自动同步没生效时手动推）
#营地共享库查 <QQ>         问库里有这个 QQ 的记录没，排查「对方说我没绑定」用
#营地共享库卸载           停掉服务，密钥和数据都保留（重新部署能接着用）
#营地共享库地址 <地址>     设置共享库地址（要带 http:// 或 https://）
#营地共享库令牌 <令牌>     设置共享库令牌
#接入营地共享库 / #关闭营地共享库
#营地共享库提醒           重新私聊发一次接入提醒
```

> 这条「状态」为什么不能省掉「库」字：`#营地共享状态` 是**用户**在用的（看自己开没开共享），
> 两边只差一个字，混了就会互相抢。

自己搭了库、别人要接入的流程：`#营地共享库部署` → 拿到自己那个令牌接上自己的机器人 →
别人找你要时发 `#营地共享库发令牌 某某的机器人`，把回复里那三行整段转给对方就行。

在群里想省掉转发这一步，就直接 `#营地共享库发令牌 @某人`：机器人会把令牌私聊发给 TA
（备注可以省，默认用 TA 的昵称）。对方没开临时会话、或者没加机器人好友时发不出去，
这种情况令牌会退回私聊给主人，群里只说一句「没发出去」—— 令牌永远不出现在群里。

首次安装和每次更新后，插件会私聊主人问一次要不要接入（已接入或已经说过不要的不会再问）。

## 其它

```
#导出战绩            导出成 CSV，拿去 Excel 自己拉透视表
#王者备份            打包配置与归档（含凭证，只私聊发）
#王者缓存状态 #清理王者缓存
#王者帮助 #王者设置 #王者更新
```

## 拉黑某人（主人）

```
#王者拉黑@某人        也可以写 QQ 号：#王者拉黑 10001
#王者取消拉黑@某人
#王者黑名单           看当前名单，锅巴面板里也能直接改
```

名单里的人发任何王者指令都不会有回应，之前订阅的战绩推送、上下线提醒、日周月报也不再推，
群报和排行榜里也不统计他。订阅和绑定的数据一个都不删，移出名单就恢复原样，主人不受影响。

默认还会跟随机器人自身的黑名单（`config/config/other.yaml` 的 `blackUser` / `blackQQ`）：
那份名单只挡用户主动发的指令，挡不住插件按时间主动发的推送，所以「人早就拉黑了、战绩推送还在推」
这种情况靠它一并停掉。不想跟随可以在 `#王者设置` 里关掉。

## 致谢

- [@Tloml-Starry/GloryOfKings-Plugin](https://gitee.com/Tloml-Starry/GloryOfKings-Plugin) —— 原插件
- [@KimigaiiWuyi/WzryUID](https://github.com/KimigaiiWuyi/WzryUID) —— 营地接口调用逻辑参考
- 数据来自王者营地与王者荣耀官网资料库

觉得有用点个 Star ⭐

